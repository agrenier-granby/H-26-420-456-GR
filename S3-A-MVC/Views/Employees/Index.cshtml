@model S3_A_MVC.ViewModels.EmployeesIndexVM

@{
    var cssClasses = Model.Employees.Count >= 5 ? "alert alert-success" : "";
}

<h2 class="@cssClasses">Liste des employés</h2>

@foreach (var employee in Model.Employees)
{
    @await Html.PartialAsync("_EmployeeInfoPartial", employee)
}